<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for logging to stdout -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for persistent logging -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/ojp-server.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>logs/ojp-server.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep 30 days of history -->
            <maxHistory>30</maxHistory>
            <!-- Cap total size at 1GB -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Specific logger configurations -->
    <!-- Reduce verbosity of gRPC -->
    <logger name="io.grpc" level="WARN"/>
    
    <!-- Reduce verbosity of Netty -->
    <logger name="io.netty" level="WARN"/>
    
    <!-- OpenTelemetry logging -->
    <logger name="io.opentelemetry" level="INFO"/>
    
    <!-- OJP specific logging - can be adjusted as needed -->
    <logger name="org.openjproxy" level="INFO"/>
</configuration>
